apiVersion: v1
kind: Service
metadata:
  name: postgres-database
  labels:
    app: postgres-database
spec:
  type: NodePort
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432

  selector:
    app: postgres-database